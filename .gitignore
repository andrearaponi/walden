# Compiled binaries
walden-test
walden-*-linux-amd64
walden-*-linux-arm64
walden-*-darwin-amd64
walden-*-darwin-arm64

# macOS
.DS_Store
.DS_Store?
._*

# Claude Code local config
.claude/

# Walden dogfooding artifacts (local only — never published in this repo)
.walden/
.github/workflows/validate-walden.yml
.github/pull_request_template.md

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Bob the Fixer local configuration
bobthefixer.env
# SonarQube local configuration
sonar-project.properties

# Local-only material (reports, demo assets, launch notes)
temp/
HACKER_NEWS.md
IDEAS.md

# Agent session exports (never part of the public source)
pi-session-*.html

# Private experimental skill drafts (only skill/walden and skill/walden-history are published)
skill/walden-soundings/
skill/walden-soundings.zip

# Local coverage output
coverage.out

# Generated docs section (built by tools/sitegen at Pages deploy time)
site/docs/
